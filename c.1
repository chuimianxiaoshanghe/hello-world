
#include<stdio.h>
int main()
{
	int a = 0, b = 0, c = 0;
	printf("请输入三个不相等的数字\n");
	scanf("%d %d %d", &a, &b, &c);
	if (a > b)
	{
		if (a > c && b > c)
			printf("%d %d %d\n", a, b, c);
		else if (a > c && b < c)
			printf("%d %d %d\n", a, c, b);
		else 
			printf("%d %d %d\n", c, a, b);
	}
	else
	{
		if (b>c&&a>c)
			printf("%d %d %d\n", b, a, c);
		else if (b>c&&c>a)
			printf("%d %d %d\n", b, c, a);
		else
			printf("%d %d %d\n", c, b, a);
	}
	return 0;
}
